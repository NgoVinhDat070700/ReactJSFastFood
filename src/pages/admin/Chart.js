import "./home.scss";
import {
  LineChart,
  Line,
  XAxis,
  CartesianGrid,
  Tooltip,
  ResponsiveContainer,
  YAxis,
} from "recharts";
import { Legend } from "chart.js";

export default function Chart() {

  return (
    <h1>Welcome bạn đến với Dashboard</h1>
  );
}